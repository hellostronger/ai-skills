# CoolClaws Book Pattern - Detailed Templates

## 1. Chapter Content Templates

### Template A: "What is X" Chapter (Chapter 1)

```markdown
# 第 1 章 <项目名> 是什么，为什么重要

## 1.1 从名字说起

<项目全称> 的全称是 **<全称展开>**，由 <组织/作者> 开源，是一个 **<项目类型定义>**——<一句话解释项目本质>。

<发布历史，如：2026 年 2 月 28 日，<项目> 2.0 发布当天登顶 GitHub Trending 第一名。>

## 1.2 <类别 A> vs <类别 B>：本质区别

很多人把 <项目名> 和 <竞品 1>、<竞品 2> 这类 <类别> 相提并论，但两者的设计哲学完全不同。

| 维度 | <类别 A> | <类别 B> |
|------|----------|----------|
| 设计理念 | <描述> | <描述> |
| 执行环境 | <描述> | <描述> |
| 决策权 | <描述> | <描述> |
| 扩展方式 | <描述> | <描述> |
| Opinionated 程度 | <描述> | <描述> |

<项目名> 是 **opinionated** 的：它预设了 <设计决策 1>、预设了 <设计决策 2>、预设了 <设计决策 3>。你可以改，但不改也能直接跑。这就是 "batteries included" 的含义。

## 1.3 <项目> 的三个核心特征

<项目名> 瞄准的是 **<目标场景>**——<场景描述>。

**特征一：<特征名>。** <详细解释，包含具体数据或例子>

**特征二：<特征名>。** <详细解释，引用源码中的关键设计>

```python
# 摘自 <文件路径>
<关键代码片段>
```

这段代码体现了 <项目名> 的核心理念：<理念总结>。

**特征三：<特征名>。** <详细解释>

## 1.4 发展历程

<项目> v1 诞生于 <时间>，定位是 <初始定位>。它做的事情很简单：<简单描述>。

然后社区做了 <项目> 团队没有预料到的事情：
- 有人用它做 <意外用例 1>
- 有人用它做 <意外用例 2>
- 有人用它做 <意外用例 3>

这些用例让团队意识到：<洞察>。

于是在 <时间>，团队做了一个大胆的决定：**从头重写**。<项目> 2.0 与 v1 不共享任何代码。

## 1.5 实际用例

**用例一：<用例名>。** <描述，说明输入和输出>

**用例二：<用例名>。** <描述>

**用例三：<用例名>。** <描述，包含社区案例>

这些用例的共同特点是：<总结共同特征>。

## 1.6 与其他产品的定位对比

| 产品 | 定位 | 交互方式 | 开源 |
|------|------|---------|------|
| **<项目名>** | <定位> | <方式> | 是（<协议>） |
| <竞品 1> | <定位> | <方式> | 否 |
| <竞品 2> | <定位> | <方式> | 否 |

<项目名> 的核心差异在于：它是**开源的**、**可自托管的**、**面向开发者的**。

## 小结

- **<项目名> 是一个 <类型定义>**，不是 <容易混淆的类型>——它提供 <核心价值>。
- **<数字> 个核心特征**：<特征 1>、<特征 2>、<特征 3>。
- **<发展历程要点>**。
- **<差异化优势>**。
```

### Template B: "Core Engine" Chapter (Chapters 4-7)

```markdown
# 第 N 章 <引擎/模块名>：<副标题>

<引言段落：概述本章内容，说明该模块在整体架构中的位置和作用>

## N.1 <基础概念> 是什么

<概念名> 是 <来源> 推出的 <概念类型>。它的核心抽象是 **<核心抽象名>**——<解释>：

- **<元素 1>**：<描述>
- **<元素 2>**：<描述>
- **<元素 3>**：<描述>

与传统的 <替代方案> 不同，<概念名> 原生支持**<关键特性>**——这正是 <场景> 所需要的。

为什么不用简单的 <简单方案> 来实现？因为 <复杂方案> 带来了三个关键能力：第一，<能力 1>；第二，<能力 2>；第三，<能力 3>。

## N.2 <项目名> 如何使用 <概念>

<项目名> 没有直接操作 <底层 API>，而是使用了 <高层封装>。这个函数内部自动构建了 <内部结构>。<项目名> 通过 <中间层> 对其进行了一层封装：

```python
# 简化后的调用链
def <函数名>(<参数>):
    return <调用>(
        <参数 1>=<值>,
        <参数 2>=<值>,
        <参数 3>=<值>,
    )
```

`<函数名>()` 最终调用 `<底层函数>()`，将 <项目名> 的 <组件 1>、<组件 2>、<组件 3> 传入。返回的是一个 <返回类型> 对象，可以直接 `<方法>()` 执行。

这种设计的好处是：<好处 1>；<好处 2>；<好处 3>。

## N.3 <状态/数据模型>

<项目名> 的 <模块> 默认使用 `<默认状态>` 作为状态模式，它只包含一个 `<字段>` 字段用于存储 <数据>。但 <项目名> 的 <模块> 需要管理远比 <数据> 更多的运行时数据。因此，`<自定义状态>` 继承了 `<默认状态>` 并扩展了 <数字> 个自定义字段：

```python
class <自定义状态>(<基类>):
    <字段 1>: NotRequired[<类型> | None]
    <字段 2>: NotRequired[<类型> | None]
    <字段 3>: Annotated[list[<类型>], <合并函数>]
    <字段 4>: NotRequired[list | None]
```

各字段的职责如下：

| 字段 | 类型 | 说明 |
|------|------|------|
| `<字段 1>` | `<类型>` | <说明>，由 `<组件>` 管理生命周期 |
| `<字段 2>` | `<类型>` | <说明>，包含 <子字段> 三个路径 |
| `<字段 3>` | `<类型>` | <说明>，使用 reducer 去重合并 |

## N.4 自定义 <处理函数>

在 <框架> 中，当多个 <组件> 对同一个 <数据> 进行更新时，需要一个**合并策略**来决定如何处理新旧值。这就是 <处理函数> 的作用。

**`<函数名 1>`：<策略描述>**

```python
def <函数名 1>(existing: <类型> | None, new: <类型> | None) -> <类型>:
    if existing is None:
        return new or []
    if new is None:
        return existing
    return list(dict.fromkeys(existing + new))
```

这个函数用 `<实现方式>` 实现 <策略>——<解释原理>。这意味着当 <场景> 时，最终结果既 <优点 1>，也 <优点 2>。

## 小结

- **<要点 1>**：<总结>
- **<要点 2>**：<总结>
- **<要点 3>**：<总结>
- **<要点 4>**：<总结>
```

### Template C: "Extension System" Chapter (Chapters 11+)

```markdown
# 第 N 章 <扩展系统名>：<副标题>

<引言段落：说明扩展系统的作用，为什么需要它，它解决什么问题>

## N.1 为什么需要 <扩展系统>

<项目名> 的 <核心系统> 覆盖了 <基本场景>，但实际生产中，用户往往需要：<需求 1>、<需求 2>、<需求 3>。

如果把这些需求都塞进核心系统，会导致：<问题 1>、<问题 2>、<问题 3>。

<扩展系统> 的设计哲学是：**<哲学总结>**。核心保持精简，可选功能通过 <扩展机制> 实现。

## N.2 <扩展系统> 的架构

<扩展系统> 由三个核心组件构成：

```
┌─────────────────────────────────┐
│         <扩展系统>               │
├─────────────────────────────────┤
│  <组件 1>  │  <组件 2>  │ <组件 3> │
└─────────────────────────────────┘
```

### <组件 1>

<详细描述>

```python
class <组件 1>:
    def <方法>(self, <参数>):
        <实现>
```

### <组件 2>

<详细描述>

## N.3 内置 <扩展类型>

<项目名> 提供了 <数字> 个内置 <扩展类型>：

| <扩展类型> | 功能 | 使用场景 |
|-----------|------|---------|
| `<名称 1>` | <功能> | <场景> |
| `<名称 2>` | <功能> | <场景> |
| `<名称 3>` | <功能> | <场景> |

### <扩展 1> 深度解析

<源码引用>

```python
# 摘自 <文件路径>
class <扩展类>(<基类>):
    """<文档字符串>"""
    
    def <方法>(self, <参数>):
        <实现>
```

### <扩展 2> 深度解析

<源码引用>

## N.4 编写自定义 <扩展类型>

<步骤说明>

```python
# 示例：自定义 <扩展类型>
from <项目名> import <基类>

class My<扩展类型>(<基类>):
    name = "my-<扩展类型>"
    
    def <必须实现的方法>(self, <参数>):
        <实现>
```

<配置说明>

```yaml
# 在配置文件中注册
<扩展系统>:
  - name: my-<扩展类型>
    enabled: true
```

## 小结

- **<要点 1>**：<总结>
- **<要点 2>**：<总结>
- **<要点 3>**：<总结>
```

---

## 2. Index Page Templates

### Hero Section

```yaml
---
layout: home

hero:
  name: "<项目名> 源码解析"
  text: "<一句话副标题>"
  tagline: <详细描述，50-80 字，说明覆盖范围和读者将获得什么>
  actions:
    - theme: brand
      text: 开始阅读
      link: /chapters/01-<first-chapter>
    - theme: alt
      text: 查看目录
      link: /contents
    - theme: alt
      text: GitHub
      link: https://github.com/<username>/<project>-book

features:
  - icon:
      src: /icons/<feature1>.svg
    title: <特性 1 标题>
    details: <特性 1 描述，50-80 字>

  - icon:
      src: /icons/<feature2>.svg
    title: <特性 2 标题>
    details: <特性 2 描述，50-80 字>

  - icon:
      src: /icons/<feature3>.svg
    title: <特性 3 标题>
    details: <特性 3 描述，50-80 字>

  - icon:
      src: /icons/<feature4>.svg
    title: <特性 4 标题>
    details: <特性 4 描述，50-80 字>
---
```

---

## 3. Appendix Templates

### Appendix A: Reading Path Guide

```markdown
# 附录 A 阅读路径指南

本书涵盖 <项目名> 的架构设计、核心实现与生产运维，内容较多。根据你的角色和目标，推荐以下三条阅读路径。

## 路径一：入门开发者

**推荐章节：第 1-3 章，然后第 4-5 章**

如果你是第一次接触 <项目名>，建议从第 1 章的项目概览开始，了解 <项目名> 是什么、解决什么问题。第 2 章和第 3 章带你完成环境搭建和第一次运行，建立直观体验。有了基础认知后，阅读第 4-5 章深入理解 <核心概念 1> 和 <核心概念 2>。这条路径的目标是让你能够独立配置和使用 <项目名>，并对系统有一个完整的全局认知。

## 路径二：架构师 / 深度开发者

**推荐章节：第 6-11 章**

如果你已经熟悉 <项目名> 的基本用法，想要深入理解或定制扩展系统，重点阅读第 6 章 <核心模块 1>、第 7-8 章的 <核心模块 2>、第 9-11 章的 <扩展系统>。这些章节覆盖了 <项目名> <核心能力> 的完整设计——从 <环节 1>、<环节 2> 到 <环节 3> 的完整链路。读完这些章节，你将具备为 <项目名> 开发自定义 <扩展类型>、编写新 <组件> 或集成第三方 <服务> 的能力。

## 路径三：运维 / DevOps 工程师

**推荐章节：第 14-15 章，第 20-22 章**

如果你的职责是部署和运维 <项目名>，可以直接跳到第 14-15 章了解 <基础设施>，然后阅读第 20-22 章的 <配置>、<模型适配> 和 <部署>。第 20 章的 <速查表> 和附录 B 的 <参考表> 是日常运维的高频参考资料。第 22 章详细讲解了 <部署方式>、<反向代理>、<监控追踪> 和 <性能调优>，覆盖了从搭建到监控的全流程。
```

### Appendix B: Configuration Reference

```markdown
# 附录 B 配置字段速查表

本附录列出 <项目名> 所有可配置字段及其说明。配置支持三种方式：
1. 环境变量（`<PREFIX>_<FIELD_NAME>`）
2. 配置文件（`config.yaml`）
3. 命令行参数（`--<field-name>`）

优先级：命令行参数 > 环境变量 > 配置文件 > 默认值

## 核心配置

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `<field_1>` | `<type>` | `<default>` | <说明> |
| `<field_2>` | `<type>` | `<default>` | <说明> |

## <模块名> 配置

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `<field_1>` | `<type>` | `<default>` | <说明> |

## 示例配置

```yaml
# 完整配置示例
<module_1>:
  <field_1>: <value>
  <field_2>: <value>

<module_2>:
  <field_1>: <value>
```
```

### Appendix C: Glossary

```markdown
# 附录 C 术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| <术语 1> | <English> | <解释> |
| <术语 2> | <English> | <解释> |
| <术语 3> | <English> | <解释> |
```

---

## 4. ASCII Diagram Patterns

### Architecture Overview

```
┌─────────────────────────────────────────────┐
│              <系统名称>                       │
├─────────────────────────────────────────────┤
│                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 模块 A    │  │ 模块 B    │  │ 模块 C    │  │
│  └──────────┘  └──────────┘  └──────────┘  │
│       │              │              │        │
│       └──────────────┼──────────────┘        │
│                      │                       │
│              ┌──────────┐                    │
│              │ 核心引擎  │                    │
│              └──────────┘                    │
│                      │                       │
│       ┌──────────────┼──────────────┐        │
│       │              │              │        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 存储层    │  │ 缓存层    │  │ 通信层    │  │
│  └──────────┘  └──────────┘  └──────────┘  │
│                                             │
└─────────────────────────────────────────────┘
```

### Pipeline / Flow

```
<输入> → <步骤 1> → <步骤 2> → <步骤 3> → <输出>
           │           │           │
           ▼           ▼           ▼
        <副作用 1>  <副作用 2>  <副作用 3>
```

### Layer Architecture

```
┌─────────────────────────────┐
│     Presentation Layer       │  ← 用户接口
├─────────────────────────────┤
│     Business Logic Layer     │  ← 核心逻辑
├─────────────────────────────┤
│     Data Access Layer        │  ← 数据操作
├─────────────────────────────┤
│     Infrastructure Layer     │  ← 基础设施
└─────────────────────────────┘
```

---

## 5. Comparison Table Patterns

### Product Comparison

```markdown
| 维度 | <项目名> | <竞品 1> | <竞品 2> |
|------|----------|----------|----------|
| 定位 | <定位> | <定位> | <定位> |
| 语言 | <语言> | <语言> | <语言> |
| 许可证 | <许可证> | <许可证> | <许可证> |
| 核心特性 | <特性> | <特性> | <特性> |
| 学习曲线 | <描述> | <描述> | <描述> |
| 社区活跃度 | <描述> | <描述> | <描述> |
```

### Approach Comparison

```markdown
| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| <方案 A> | <优点> | <缺点> | <场景> |
| <方案 B> | <优点> | <缺点> | <场景> |
| <方案 C> | <优点> | <缺点> | <场景> |
```

---

## 6. Code Snippet Patterns

### Simple Function

```python
# 摘自 <文件路径>
def <函数名>(<参数>: <类型>) -> <返回类型>:
    """<文档字符串>"""
    <实现>
```

### Class Definition

```python
# 摘自 <文件路径>
class <类名>(<基类>):
    """<文档字符串>"""
    
    def __init__(self, <参数>):
        self.<属性> = <值>
    
    def <方法名>(self, <参数>) -> <返回类型>:
        <实现>
```

### Configuration

```yaml
# config.yaml 示例
<模块名>:
  <字段 1>: <值>
  <字段 2>: <值>
  <嵌套配置>:
    <字段 3>: <值>
```

---

## 7. Chapter Title Patterns

### Descriptive Titles

- 第 1 章 <项目名> 是什么，为什么重要
- 第 2 章 仓库全景与技术栈
- 第 3 章 快速上手
- 第 4 章 <引擎名>：<动词><名词>的<名词>
- 第 5 章 <模块名>：<比喻>的<名词>
- 第 6 章 <数字>层<系统名>：<比喻>
- 第 7 章 <概念名>：<中文翻译>
- 第 8 章 <系统名>架构总览
- 第 9 章 <执行器名>：执行引擎
- 第 10 章 并发调度与 <策略名> 策略
- 第 11 章 <系统名>架构
- 第 12 章 <系统名>流水线
- 第 13 章 <抽象层名>抽象层
- 第 14 章 <实现 1> 与 <实现 2> 深度解析
- 第 15 章 内置工具与社区工具
- 第 16 章 <扩展协议> 扩展：无限<名词>
- 第 17 章 <系统名>系统：能力的核心扩展单元
- 第 18 章 编写自定义 <扩展类型>
- 第 19 章 <网关名> Gateway
- 第 20 章 <渠道名> 渠道系统
- 第 21 章 配置体系全解
- 第 22 章 模型配置与适配
- 第 23 章 部署与生产化

### Appendix Titles

- 附录 A：阅读路径指南
- 附录 B：<类型> 字段速查表
- 附录 C：术语表
- 附录 D：版本更新日志
